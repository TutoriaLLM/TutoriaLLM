{
  "name": "kids-code-tutorial",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "client-dev": "cd client && npm run dev",
    "client-build": "cd client && npm run build",
    "dev": "tsx watch ./index.ts",
    "start": "tsc && npm run client-build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "client",
    "backend"
  ],
  "private": true,
  "author": "So Tokumaru",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.12",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5"
  }
}
